---
import "@fontsource/josefin-sans";
import "@fontsource/great-vibes";
import Ornamento1 from "../../../assets/icons/Ornamento1.astro";

const { fecha, colorPrimario, colorSecundario } = Astro.props;

const eventDate = new Date(fecha.fecha_iso);
const year = eventDate.getFullYear();
const month = eventDate.getMonth();
const eventDay = eventDate.getDate();

const firstDayRaw = new Date(year, month, 1).getDay();
const firstDay = firstDayRaw === 0 ? 6 : firstDayRaw - 1;
const daysInMonth = new Date(year, month + 1, 0).getDate();
const diasSemana = ["Lu", "Ma", "Mi", "Ju", "Vi", "Sa", "Do"];

const cells: (number | null)[] = [
  ...Array(firstDay).fill(null),
  ...Array.from({ length: daysInMonth }, (_, i) => i + 1),
];
---

<section class="h-full flex flex-col items-center justify-center px-6">
  <div
    class="flex flex-col items-center gap-4 text-center px-6 py-8 rounded-3xl w-full max-w-sm"
    style="background: rgba(255,255,255,0.12); backdrop-filter: blur(18px); -webkit-backdrop-filter: blur(18px); border: 1px solid rgba(255,255,255,0.25);"
  >
    <div data-aos="fade-down" data-aos-duration="800">
      <h2
        class="text-4xl"
        style={`font-family: 'Great Vibes', cursive; color: ${colorPrimario};`}
      >
        {fecha.mes} {fecha.anio}
      </h2>
    </div>

    <div data-aos="zoom-in" data-aos-duration="800" data-aos-delay="100">
      <Ornamento1 style={`fill: ${colorPrimario};`} class="w-24 opacity-70" />
    </div>

    <div
      class="w-full max-w-xs"
      data-aos="fade-up"
      data-aos-duration="1000"
      data-aos-delay="200"
    >
      <div class="grid grid-cols-7 mb-2">
        {diasSemana.map((d) => (
          <div
            class="text-center text-xs font-bold uppercase tracking-wider py-1"
            style={`font-family: 'Josefin Sans', sans-serif; color: ${colorPrimario}; opacity: 0.6;`}
          >
            {d}
          </div>
        ))}
      </div>

      <div class="grid grid-cols-7 gap-y-1">
        {cells.map((day) => (
          <div class="flex items-center justify-center aspect-square">
            {day === null ? null : day === eventDay ? (
              <div
                class="w-9 h-9 rounded-full flex items-center justify-center text-sm font-bold shadow-md"
                style={`background-color: ${colorPrimario}; color: ${colorSecundario}; font-family: 'Josefin Sans', sans-serif;`}
              >
                {day}
              </div>
            ) : (
              <span
                class="text-sm"
                style={`font-family: 'Josefin Sans', sans-serif; color: ${colorPrimario}; opacity: 0.7;`}
              >
                {day}
              </span>
            )}
          </div>
        ))}
      </div>
    </div>

    <p
      class="text-xs tracking-widest uppercase"
      style={`font-family: 'Josefin Sans', sans-serif; color: ${colorPrimario}; opacity: 0.7;`}
      data-aos="fade-up"
      data-aos-duration="800"
      data-aos-delay="400"
    >
      {fecha.dia} de {fecha.mes} · {fecha.anio}
    </p>
  </div>
</section>
