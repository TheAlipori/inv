---
interface Props {
  fecha: {
    dia: string;
    mes: string;
    anio: string;
  };
  novios: {
    novio: string;
    novia: string;
  };
  padrinos: {
    padrino: string;
    madrina: string;
  };
  colorPrimario: string;
}
import "@fontsource/abril-fatface";
import "@fontsource/whisper";
import "@fontsource/libre-baskerville";
import Ornamento1 from "../../../assets/icons/Ornamento1.astro";

const { fecha, novios, padrinos, colorPrimario } = Astro.props;
---

<section class="py-4 bg-white">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-5 gap-4 md:gap-8 items-center max-w-4xl mx-auto">
      <!-- N�meros de fecha en vertical (izquierda) -->
      <div
        class="col-span-1 flex flex-col items-center justify-center space-y-4"
        data-aos="fade-right"
        data-aos-duration="1000"
      >
        <div
          class="text-4xl md:text-3xl font-Libre font-black text-gray-900 leading-none"
        >
          26
        </div>
        <div
          class="text-4xl md:text-3xl font-Libre font-black leading-none"
          style={`color: ${colorPrimario}`}
        >
          12
        </div>
        <div
          class="text-4xl md:text-3xl font-Libre font-black text-gray-900 leading-none"
        >
          25
        </div>
      </div>

      <!-- Mensaje de bienvenida (derecha) -->
      <div
        class="col-span-4 text-right"
        data-aos="fade-left"
        data-aos-duration="1000"
        data-aos-delay="300"
      >
        <p class="text-sm text-gray-800 leading-relaxed font-Libre italic mb-6">
          Nos llena de una inmensa alegría invitarte a compartir el día más
          importante de nuestras vidas. Tu presencia será el regalo más valioso
          y significativo que podamos recibir en este capítulo tan especial.
          ¡Tenerte con nosotros hará que este momento sea aún más inolvidable!
        </p>
      </div>
    </div>
  </div>

  <!-- Nombres de la pareja (nueva sección) -->
  <div class="text-center mt-4">
    <div data-aos="fade-up" data-aos-duration="1000" data-aos-delay="600">
      <div
        class="text-6xl font-Libre text-gray-800 uppercase leading-none -mb-4"
      >
        MAGUI
      </div>
      <div
        class="font-Whisper text-7xl leading-none -my-5"
        style={`color: ${colorPrimario}`}
      >
        &
      </div>
      <div
        class="text-6xl font-Libre text-gray-800 uppercase leading-none -mt-5"
      >
        NOÉ
      </div>
    </div>
  </div>

  <!-- Nombres completos y padrinos -->
  <div class="container mx-auto px-4 mt-8">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Novios completos -->
      <div
        class="mb-8"
        data-aos="fade-up"
        data-aos-duration="1000"
        data-aos-delay="800"
      >
        <h3
          class="text-6xl font-Whisper mb-4"
          style={`color: ${colorPrimario}`}
        >
          Los Novios
        </h3>
        <p class="text-lg font-Libre text-gray-800 font-medium mb-2">
          {novios.novia}
        </p>
        <p class="text-lg font-Libre text-gray-800 font-medium">
          {novios.novio}
        </p>
        <div class="flex justify-center mt-4">
          <Ornamento1 class="size-44 -mt-20" style={`fill: ${colorPrimario}`} />
        </div>
      </div>

      <!-- Padrinos -->
      <div
        class="-mt-20"
        data-aos="fade-up "
        data-aos-duration="1000"
        data-aos-delay="1000"
      >
        <h3
          class="text-6xl font-Whisper mb-4"
          style={`color: ${colorPrimario}`}
        >
          Padrinos de boda
        </h3>
        <p class="text-lg font-Libre text-gray-800 font-medium mb-2">
          {padrinos.madrina}
        </p>
        <p class="text-lg font-Libre text-gray-800 font-medium">
          {padrinos.padrino}
        </p>
        <div class="flex justify-center mt-4">
          <Ornamento1 class="size-44 -mt-20" style={`fill: ${colorPrimario}`} />
        </div>
      </div>
    </div>
  </div>
</section>
